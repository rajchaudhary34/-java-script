"use strict"; // use newer version 
// alert(3+3) works in browser not in node.js

let age =89
let isloggedin= false
let  name = "raj"

console.log(typeof(age))
console.log(typeof(null)) // object
console.log(typeof(undefined)) // undefined